export function col(
  arr: any[][],
  col: number,
): [];

export function row(
  arr: any[][],
  row: number,
): [];

export function diagonal(
  arr: any[][],
  row: number,
  col: number,
): [];

export function antidiagonal(
  arr: any[][],
  row: number,
  col: number,
): [];

export function area(
  arr: any[][],
  cell1: [number, number],
  cell2: [number, number],
): [][] | [];